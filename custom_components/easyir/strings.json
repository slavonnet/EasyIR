{
  "config": {
    "abort": {
      "zha_not_configured": "Configure ZHA first, then add EasyIR."
    },
    "error": {
      "invalid_device": "Selected device was not found.",
      "unknown_ieee": "Could not read Zigbee IEEE from this device. Pick another ZHA device.",
      "invalid_profile": "Pick a built-in profile or enter a valid custom file path."
    },
    "step": {
      "user": {
        "title": "EasyIR",
        "description": "Pick your ZHA IR device (picker prefers TS1201 models). Built-in profiles ship with EasyIR; custom path is optional.%optional_supported",
        "data": {
          "zha_device": "ZHA device",
          "profile_choice": "IR profile",
          "profile_path": "Custom profile path (only if you chose Custom path)",
          "endpoint_id": "Default endpoint id"
        }
      }
    }
  },
  "services": {
    "learn_once": {
      "name": "Learn IR code from hub",
      "description": "Enable IR learn mode on a supported hub, wait for one remote button press, read learned code, then rely on hub auto-exit from learn mode.",
      "fields": {
        "ieee": {
          "name": "IEEE",
          "description": "Optional if default IEEE is set via integration UI setup."
        },
        "endpoint_id": {
          "name": "Endpoint id",
          "description": "Optional endpoint id (default 1)."
        },
        "timeout_s": {
          "name": "Timeout seconds",
          "description": "Maximum wait time for one learned code."
        }
      }
    }
  }
}
